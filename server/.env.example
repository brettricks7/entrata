# Server config
PORT=4000
CORS_ORIGIN=http://localhost:5173
# CACHE_DIR=server/cache

# LLM config
LLM_ALLOW_NETWORK=true
LLM_MODEL=gemini-flash-latest
CACHE_VERSION=v1

# Unlock TTL (2 hours)
LLM_UNLOCK_TTL_MS=7200000

# Gemini API key (optional if using vault unlock)
GEMINI_API_KEY=

# Optional: Prisma Postgres (unused by MVP)
DATABASE_URL=